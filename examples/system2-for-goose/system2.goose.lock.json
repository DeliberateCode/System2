{
  "backend": "goose",
  "goose_version_assumed": "1.38.0",
  "mode": "smart_approve",
  "permission_delivery": "ephemeral-xdg-config",
  "DEGRADATION": "On Goose, enforced safety primitives are DOWNGRADED: block-dangerous/protect-sensitive are best-effort permission gates (adapted, NOT enforced \u2014 the launcher delivers them via an ephemeral XDG_CONFIG_HOME permission.yaml + smart_approve, NOT native blocking); enforce-lease/format/typecheck/budget are advisory recipe instructions only (NOT enforced). adapted != enforced.",
  "capabilities": {
    "enforce-lease": {
      "status": "advisory",
      "mechanism": "recipe instruction block only: Goose has no per-path write gate (no PreToolUse hook, no project-scoped permission). The lease/write-scope is stated as an instruction the model is asked to honor; it is NOT blocked. LOUD DEGRADATION: native enforced lease -> advisory.",
      "enforced": false,
      "gated": false
    },
    "block-dangerous": {
      "status": "adapted",
      "mechanism": "generated Goose permission policy (permission.yaml user section: shell/Bash tool -> never_allow for the dangerous set, ask_before otherwise) delivered via an ephemeral XDG_CONFIG_HOME config dir (the launcher copies it + the user's config.yaml into a throwaway dir; ~/.config/goose is never mutated) + GOOSE_MODE=smart_approve LLM PermissionJudge. Approximation, not the deterministic regex blocker: a permission decision, not a hard PreToolUse exit. DEGRADATION: native -> adapted.",
      "enforced": false,
      "gated": true
    },
    "protect-sensitive": {
      "status": "adapted",
      "mechanism": "generated Goose permission policy (Read/Write/Edit -> ask_before; sensitive paths -> never_allow where expressible) delivered via an ephemeral XDG_CONFIG_HOME config dir (the launcher copies it + the user's config.yaml into a throwaway dir; ~/.config/goose is never mutated) + smart_approve PermissionJudge. Goose permissions cannot path-scope as precisely as the sensitive-file-protector; coverage is approximate. DEGRADATION: native -> adapted.",
      "enforced": false,
      "gated": true
    },
    "format": {
      "status": "advisory",
      "mechanism": "recipe instruction block only: Goose has no PostToolUse hook, so post-edit formatting cannot be enforced. The recipe instructs the agent to format edited files. DEGRADATION: native -> advisory.",
      "enforced": false,
      "gated": false
    },
    "typecheck": {
      "status": "advisory",
      "mechanism": "recipe instruction block only: Goose has no PostToolUse hook, so post-edit type-checking cannot be enforced. The recipe instructs the agent to type-check edited files. DEGRADATION: native -> advisory.",
      "enforced": false,
      "gated": false
    },
    "budget": {
      "status": "advisory",
      "mechanism": "recipe instruction block only: Goose has no SubagentStop hook, so change-budget reporting cannot be enforced. The recipe instructs the agent to report its change budget. DEGRADATION: native -> advisory.",
      "enforced": false,
      "gated": false
    }
  },
  "overlay_sources": [
    "/Users/james/DeliberateCode/System2/evals/fixtures/test-overlay"
  ]
}
